{
    "configurations": [
        {
            "name": "Attach to Chrome",
            "port": 4200,
            "request": "attach",
            "type": "pwa-chrome",
            "webRoot": "${workspaceFolder}"
        }
    ]
}